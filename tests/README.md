# Tests

Run test-policy.ps1 and test-portable.ps1 from the repository root. Run tests/integration.ps1 in an interactive Windows desktop session to verify synthetic popup dismissal and preservation of the voice window. See the main README for build commands.
